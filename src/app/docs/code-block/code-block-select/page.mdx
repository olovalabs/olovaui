export const metadata = {
  title: "Code Block Select - Olova UI",
  description:
    "A sleek code block with dropdown selector for package manager commands.",
  keywords: [
    "code block select",
    "dropdown code block",
    "package manager commands",
    "copy to clipboard",
    "react code snippet",
    "olova ui"
  ],
  openGraph: {
    title: "Code Block Select - Olova UI",
    description:
      "A sleek code block with dropdown selector for package manager commands.",
    type: "article",
    url: "/docs/code-block/code-block-select",
  },
  alternates: {
    canonical: "/docs/code-block/code-block-select",
  },
};

import CodeBlockSelectView from "./code-block-select-view";

# Code Block Select

A sleek code block with a dropdown selector for switching between package manager commands. Features a compact design with a dropdown menu for selecting the package manager.

<Tabs defaultValue="preview" className="">
  <TabsList>
    <TabsTrigger value="preview">Preview</TabsTrigger>
    <TabsTrigger value="code">Code</TabsTrigger>
  </TabsList>
  <TabsContent value="preview">
    <ComponentRenderer component={<CodeBlockSelectView />} reTrigger={true} />
  </TabsContent>
  <TabsContent value="code">
    <CodeBlock filePath="src/app/docs/code-block/code-block-select/code-block-select.tsx" />
  </TabsContent>
</Tabs>

## Usage

```tsx
import { CodeBlockSelect } from "@/components/code-block-select";

export default function Example() {
  return (
    <CodeBlockSelect
      pnpm="pnpm add olova"
      yarn="yarn add olova"
      npm="npm install olova"
      bun="bun add olova"
    />
  );
}
```

## Props

| Prop | Type | Description |
|------|------|-------------|
| `npm` | `string` | The npm command to display |
| `pnpm` | `string` | The pnpm command to display |
| `yarn` | `string` | The yarn command to display |
| `bun` | `string` | The bun command to display |

## Features

- Dropdown selector with package manager icons
- Copy to clipboard functionality
- Dark theme by default
- Compact and sleek design
- Click outside to close dropdown
