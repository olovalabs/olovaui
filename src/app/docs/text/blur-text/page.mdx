export const metadata = {
  title: "React Text Animation Components | Olova UI",
  description:
    "A comprehensive collection of animated text components including typewriter, gradient, glitch, morphing, and particle effects. Perfect for dynamic text displays.",
  keywords: [
    "React text animation",
    "animated text components",
    "typewriter effect",
    "gradient text",
    "glitch text",
    "morphing text",
    "particle text",
    "text effects library",
    "Tailwind CSS text",
    "Olova UI components",
  ],
  openGraph: {
    title: "React Text Animation Components - Olova UI",
    description:
      "Comprehensive collection of React animated text components with typewriter, gradient, glitch, and particle effects.",
    type: "article",
    url: "/docs/text",
    images: [
      {
        url: "/og-image.png",
        width: 1200,
        height: 630,
        alt: "Olova UI - Modern React Component Library",
      },
    ],
  },
  twitter: {
    card: "summary_large_image",
    title: "Text Animation Components - Olova UI",
    description:
      "Comprehensive collection of React animated text components with typewriter, gradient, glitch, and particle effects.",
    images: ["/og-image.png"],
  },
  alternates: {
    canonical: "/docs/text",
  },
};

import { BlurTextView } from "./index.tsx";

# Blur Text

A smooth blur-in animation that reveals text with a customizable blur effect and directional entrance. Characters or words fade in from blurred to sharp, creating an elegant and modern text reveal effect. Perfect for hero sections, headlines, and attention-grabbing content.

<Tabs defaultValue="preview" className="">
  <TabsList>
    <TabsTrigger value="preview">Preview</TabsTrigger>
    <TabsTrigger value="code">Code</TabsTrigger>
  </TabsList>
  <TabsContent value="preview">
    <ComponentRenderer component={<BlurTextView />} reTrigger={true} />
  </TabsContent>
  <TabsContent value="code">
    <CodeBlock filePath="src/app/docs/text/blur-text/blur-text.tsx" />
  </TabsContent>
</Tabs>

## Basic Usage

Import and use the BlurText component with customizable animation properties. Control the blur direction, animation speed, and whether to animate by characters or words.

```tsx
"use client";

import { BlurText } from "@/components/blur-text";

export function BlurTextView() {
  return (
    <div className="text-center">
      <BlurText
        delay={150}
        animateBy="words"
        direction="top"
        onAnimationComplete={() => {}}
        className="text-2xl md:text-6xl mb-8"
      >
        Love This UI?
      </BlurText>
    </div>
  );
}
```

## Custom Animation Snapshots

Create your own animation effects by defining custom animation snapshots. Combine blur, opacity, and scale transformations to achieve unique text reveal effects.

```tsx
import {
  BlurText,
  BlurTextProps,
  AnimationSnapshot,
} from "@/components/blur-text";

const customAnimation: AnimationSnapshot = {
  filter: "blur(20px)",
  opacity: 0,
  scale: 0.8,
};

<BlurText
  delay={150}
  animateBy="words"
  direction="top"
  onAnimationComplete={() => {}}
  className="text-2xl mb-8"
  animationFrom={customAnimation}
>
  Hello world
</BlurText>;
```
